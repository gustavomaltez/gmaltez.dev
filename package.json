{
  "name": "gmaltez.dev",
  "version": "1.0.0",
  "private": true,
  "description": "Personal blog and portfolio site",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gustavomaltez/gmaltez.dev.git"
  },
  "license": "ISC",
  "scripts": {
    "prepare": "deno run -A npm:husky",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:headed": "playwright test --headed",
    "e2e:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.1",
    "@playwright/test": "^1.49.1"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "deno fmt",
      "deno lint"
    ],
    "*.{json,md,css}": [
      "deno fmt"
    ]
  }
}
